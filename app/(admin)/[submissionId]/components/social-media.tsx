import { getSocialIcon } from "@/lib/utils";
import { DetailPoint } from "./detail-point";
import { Applicant } from "@prisma/client";

type Props = {
  applicant: Applicant;
};

export const SocialMedia = ({ applicant }: Props) => {};
